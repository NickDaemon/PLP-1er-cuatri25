```haskell
{MR0} maxr [] = 0
{MR1} maxr (x:xs) = max x (maxr xs)

{ML0} maxl ac [] = ac
{ML1} maxl ac (x:xs) = maxl (max x ac) xs

{R0} reverse [] = []
{R1} reverse (x:xs) = reverse xs ++ [x]

-- Quiero probar que:
∀ xs :: [Int] . maxr (reverse xs) = maxl 0 xs

-- Por induccion estructural sobre xs basta ver que valen:

1). Caso Base: P(Nil)
2). Caso inductivo. ∀x :: Int. P(xs) => P(x:xs)
con P(xs) ≡ maxr (reverse xs) = maxl 0 xs

-- Caso Base.
maxr (reverse []) = maxl 0 []
maxr [] = maxl 0 [] ≡ por {R0}
0 = maxl 0 [] ≡ por {MR0}
0 = 0 ≡ por {ML0}
-- Queda demostrado el caso base.

-- Caso Inductivo:
-- Sea 
P(xs) ≡ maxr (reverse xs) = maxl 0 xs -- HI. 
-- quiero ver que vale P(x:xs)

maxr (reverse (x:xs)) = maxl 0 (x:xs)

-- Lado izq:
maxr (reverse (x:xs))
maxr (reverse xs ++ [x])            ≡ por {R1}
max (maxr reverse xs) (maxr (x:[])) ≡ por {lema-1}
max (maxl 0 xs) (maxr (x:[]))       ≡ por {HI}
max (maxl 0 xs) (max x (maxr []))   ≡ por {MR1}
max (maxl 0 xs) (max x 0)           ≡ por {MR0}
max (maxl 0 xs)  x                  ≡ por {0 elemento neutro}
maxl x xs                           ≡ por {lema-2}

-- Lado der:
maxl 0 (x:xs)
maxl (max x 0) xs ≡ por {ML1}
maxl x xs ≡ por {0 elemento neutro}

-- Como lado izq = lado der, queda demostrado el caso inductivo.
-- Como vale el caso base y el caso inductivo queda demostrado P(xs)

-- Pruebo lema-1
∀xs, ys :: [a]. maxr (xs ++ ys) = max (maxr xs) (maxr ys)

-- Uso induccion estructural sobre xs:

-- Caso base: P(Nil)
 maxr ([] ++ ys) = max (maxr []) (maxr ys)
 maxr ys = max 0 (maxr ys) ≡ por {++0} y {MR0}
 maxr ys = maxr ys         ≡ por {0 elemento neutro}

-- Caso Inductivo:
-- Sea
P(xs) ≡ maxr (xs ++ ys) = max (maxr xs) (maxr ys), mi {HI}
-- Qvq
∀x :: a. P(x:xs)

maxr ((x:xs) ++ ys) = max (maxr (x:xs)) (maxr ys)

-- Lado izq:
maxr ((x:xs) ++ ys)
maxr (x : (xs ++ ys)) ≡ por {++1}
max x (maxr (xs ++ ys)) ≡ por {MR1}
max x (max (maxr xs) (maxr ys)) ≡ por {HI}

-- Lado der
max (maxr (x:xs)) (maxr ys)
max (max x (maxr xs)) (maxr ys) ≡ por {MR1}
max (maxr ys) (max x (maxr xs)) ≡ por {Conmutatividad}
max x (max (maxr ys) (maxr xs)) ≡ por {Asociatividad}
max x (max (maxr xs) (maxr ys)) ≡ por {Conmutatividad}

-- Como lado izq = lado der, queda demostrado el caso inductivo.
-- Luego queda probado P(xs).

-- Pruebo lema-2
∀xs :: [a]. ∀x :: a. max (maxl 0 xs)  x = maxl x xs 

-- Uso induccion estructural sobre xs, basta ver que:
Caso Base: P(Nil)
Caso inductivo: ∀y :: a. P(xs) => P(y:xs)
Con P(xs) ≡ ∀x :: a. max (maxl 0 xs) x = maxl x xs

-- Caso Base:
max (maxl 0 []) x = maxl x []
max 0 x = maxl x [] ≡ por {ML0}
x = x ≡ por {ML0} y {0 elemento neutro}
-- Queda probado el caso base

-- Caso inductivo:
-- Sea
P(xs) ≡ ∀x :: a. max (maxl 0 xs) x = maxl x xs
-- Qvq
∀y :: a. P(y:xs)

max (maxl 0 (y:xs)) x = maxl x (y:xs)

-- Lado izq:

max (maxl 0 (y:xs)) x
max (maxl (max y 0) xs) x ≡ por {ML1}
max (maxl y xs) x ≡ por {0 elemento neutro}
maxl (max y x) xs ≡ por {lema-3}

-- Lado der:
maxl x (y:xs) 
maxl (max y x) xs ≡ por {ML1}
-- Como ambos lados coinciden queda demostrado caso inductivo.

-- Pruebo lema-3
∀xs :: [a]. ∀x, y :: a. max (maxl y xs) x = maxl (max y x) xs

-- Caso Base
max (maxl y []) x = maxl (max y x) []
max y x = max y x ≡ por {ML0} x 2

-- Caso Inductivo:

max (maxl y (z:xs)) x = maxl (max y x) (z:xs)

-- Lado izq:
max (maxl y (z:xs)) x
max (maxl (max y z) xs) x ≡ por {ML1}
maxl (max (max y z) x) xs ≡ por {HI}

-- Lado Der:
maxl (max y x) (z:xs)
maxl (max (max y x) z) xs ≡ por {ML1}
maxl (max (max y z) x) xs ≡ por {asosiatividad}

-- Queda demostrado el caso inductivo.


```